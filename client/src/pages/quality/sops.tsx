export default function SopsPage() {
  return (
    <div className="text-sm text-muted-foreground" data-testid="quality-sops-placeholder">
      SOPs — create, approve, and retire standard operating procedures (coming in Task 16).
    </div>
  );
}
