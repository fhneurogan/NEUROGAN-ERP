export default function LabelingPage() {
  return (
    <div className="text-sm text-muted-foreground" data-testid="quality-labeling-placeholder">
      Labeling — artwork, spools, and reconciliation queue (coming in Task 14).
    </div>
  );
}
